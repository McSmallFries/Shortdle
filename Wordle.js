class Wordle extends Gamedle  {
  
  executeResults = function ()  {
    // grab the results.. how ??
    // - query the HTML for the share button -> add event listener to button (maybe hard)
    // - use the site's cookies / local storage to grab the data in JSON form.
    
  }
}